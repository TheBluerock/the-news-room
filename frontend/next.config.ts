import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // i18n handled by next-intl (IT / EN / ZH)
  // Sanity content fetched via GROQ API with CDN caching
};

export default nextConfig;
