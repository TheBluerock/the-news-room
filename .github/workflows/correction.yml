# services/correction/ was dissolved into services/learner/ (REF-01).
# Corrections fast-path is now handled by learner/internal/restapi and learner/internal/fastpath.
# This workflow is intentionally disabled — see learner.yml for the active pipeline.
#
# Retained as a tombstone to prevent accidental recreation of the correction service.
name: Correction Service (DISSOLVED — REF-01)
on:
  workflow_dispatch:
jobs:
  dissolved:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This service was dissolved into learner/ (REF-01). See learner.yml."
